Activation: Glob: **/*.{tsx,jsx,css}

# UI + Motion Rules
- Use shared tokens if present.
- All animation values must come from centralized motion constants.
- Reduced motion must be respected.
- Prefer reusable components over one-offs.
