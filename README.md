
  # Ecommerce Website Design

  This is a code bundle for Ecommerce Website Design. The original project is available at https://www.figma.com/design/zU6xioChbbggS8ie7GllIQ/Ecommerce-Website-Design.

  ## Running the code

  ## Environment setup

  Copy `.env.example` to `.env` and fill in your values. For the admin app, copy `admin/.env.example` to `admin/.env`.

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
